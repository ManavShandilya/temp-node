const peter  = 'peter';
const john = 'john';

// Module: Encapsulated code (only share minimum)

// The one that we have to export we will export

console.log(module);

module.exports = { john, peter }; // exporting the module values.....